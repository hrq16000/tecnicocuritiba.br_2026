import { createFileRoute } from "@tanstack/react-router";
import Diagnostico60s from "@/pages/Diagnostico60s";

export const Route = createFileRoute("/diagnostico-60s")({
  component: Diagnostico60s,
});
