import { createFileRoute } from "@tanstack/react-router";
import FunilIndisponivel from "@/pages/FunilIndisponivel";

export const Route = createFileRoute("/funil-indisponivel")({
  component: FunilIndisponivel,
});
