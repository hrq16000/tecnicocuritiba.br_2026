import { createFileRoute } from "@tanstack/react-router";
import Tindiquera from "@/pages/bairros/Tindiquera";

export const Route = createFileRoute("/bairros/tindiquera")({
  component: Tindiquera,
});
