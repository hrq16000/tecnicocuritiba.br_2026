import { createFileRoute } from "@tanstack/react-router";
import ProblemaDobradicaNotebookQuebrada from "@/pages/problemas/DobradicaNotebookQuebrada";

export const Route = createFileRoute("/problemas/dobradica-do-notebook-quebrada")({
  component: ProblemaDobradicaNotebookQuebrada,
});
